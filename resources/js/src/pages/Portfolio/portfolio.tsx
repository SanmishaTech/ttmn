import React from 'react';

const portfolio = () => {
    return (
        <div className="w-full">
            <div className="flex flex-col items-center justify-center w-full h-full bg-gray-100 p-5 rounded-lg onClick">Add Stocks</div>
        </div>
    );
};

export default portfolio;
